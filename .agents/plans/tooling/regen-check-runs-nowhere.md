---
description: "`just regen check` is called by no workflow, and two of the files it covers cannot pass it - .NET's deflate output moves between SDK patch versions and nothing pins the SDK"
state: ready
waits-on: "nothing. Answered 2026-09-04: stop byte-comparing the two ViPaq vector files and compare what they decode to. The SDK stays unpinned"
paths:
  - "tooling/**"
  - "vipaq/test-vectors/**"
  - ".github/workflows/**"
---

# The drift check nobody runs, and cannot pass

**One of four gaps that were filed together. The other three are closed.**

**What `regen` covers is settled**: data generated *into* the repository, and nothing else. The docs site's
OpenAPI copies are not that - they are hand-carried, and `just openapi check-site-copies` compares them now.

## No workflow calls it

`grep -rn regen .github/workflows/` returns nothing. The recipe that exists to catch generated data drifting
only ever runs when someone runs it by hand, which means the four generators' output can go stale on `main`
and nothing says so.

## And when it does run, it fails

`vipaq/test-vectors/interop/cs/deflate.json` and `gzip.json` come back different. Three of the fourteen
vectors in each file change - checked 2026-08-31 on .NET SDK 10.0.111.

**It is not a data problem.** The three are the ones large enough for deflate to compress rather than store,
the new bytes are shorter every time, and the ViPaq suites pass on both sets: 421 C# and 380 TypeScript
against the committed vectors, and the same numbers against the regenerated ones.

**The cause is that .NET's deflate output is not stable across runtime patch versions, and `global.json`
pins no SDK.** So the committed bytes record which SDK happened to be installed when someone last ran the
generator. Regenerating fixes it until the next SDK update, then it comes back.

## Answered 2026-09-04: compare what the vectors mean, not their bytes

**The SDK stays unpinned, deliberately.** Different SDK versions producing different deflate bytes is known
and expected behaviour, not a fault to design around. **What has to hold is that a payload keeps decoding
whatever version wrote it** - that is the property worth checking, and byte equality is a proxy for it that
gives a false negative every time the runtime moves.

`global.json` exists but sets only a test runner; no `sdk` block is added.

**So the check decodes and compares.** It is what the interop suites already do: 421 C# and 380 TypeScript
pass against the committed vectors and against regenerated ones, which is the evidence that the bytes moving
was never the problem.

**The two rejected answers, so they are not proposed again.** Committing the regenerated bytes leaves a diff
that looks real on every patch bump. Pinning the SDK makes the byte check honest by constraining every build
on every machine, which is a large cost to keep a proxy working.

**Whichever is chosen, a workflow then has to call `just regen check`** - that half is not optional and is
the reason this file exists. It belongs beside the other repo-wide checks in the pull request gate.

## Done when

**What it takes: one answer, then one change and one workflow step.**

- [ ] `just regen check` passes on a clean tree, on any SDK.
      Run it, then run it again on a different SDK patch if one is to hand. The two ViPaq vector files are
      compared by what they decode to, not byte for byte, and `global.json` still has no `sdk` block.
- [ ] Something other than a person runs it.
      `grep -rn 'regen check' .github/workflows/` matches.

Then delete this file.
