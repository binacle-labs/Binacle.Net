<?xml version="1.0" encoding="utf-8"?>
<!--
  Analysis settings for the Sonar scan, passed with /s: from .github/workflows/sonar-analysis.yml.

  This file rather than sonar-project.properties because the Scanner for .NET does not read that one - it
  ignores it silently, with no warning. This xml is its equivalent. The /s: path must be absolute.

  Token and host url stay on the command line. The token is a secret and does not belong in a committed file.

  There is deliberately no sonar.inclusions. The scanner builds its module list from the build itself, so a
  source glob can only fight it: an inclusion of `src/**/*` in the SonarCloud UI, left over from a flat
  layout, is what made the 2026-08-07 run index 0 files and still report success. Scope is exclusions only.
-->
<SonarQubeAnalysisProperties xmlns="http://www.sonarsource.com/msbuild/integration/2015/1">

  <!-- C# reaches the scanner through the build. TS reaches it through multi-language analysis, which walks the
       working tree instead, so this list is mostly about keeping that walk off generated and vendored files.

       The test for every entry: did we write it, do we review it, can we change it? If no, it goes here.
       If yes, it stays in scope even when its findings are unwelcome.

       - Build output: bin/, obj/, artifacts/, results/, and the site equivalents sites/*/js and sites/*/media
         (webpack bundles and copied assets). All gitignored, so CI never sees them anyway.
       - Vendored: assets/lib/ and its build-time copies under sites/*/lib - alpine, beercss,
         material-dynamic-colors, swagger-ui. 8157 ncloc and 274 issues in the 2026-08-08 run, none ours to fix,
         and 2329 uncovered lines that were most of the missing coverage.
       - Vendored: ruby/vendor/, which CI creates - `bundler-cache: true` installs the bundle into
         vendor/bundle before the scan. 3524 ncloc and 65 issues in the 2026-08-27 run, including every one of
         the 8 open security findings and 12 of the 34 HIGH. They are other people's gems: the gate was failing
         on kramdown's html fixtures and on unpinned actions in http_parser.rb's own workflows.
       - Not code: shared/data/ and vipaq/test-vectors/, the two fixture corpora. shared/data was the whole
         json=14010 slice of that run, 28% of the project measured as data files.
       - Not code: ruby/*/spec/fixtures/, the gems' own fixture sites. Broken on purpose - a rspec case named
         nasty.html exists to feed the parser a stray </script>, and Sonar reported it as a defect.
       - Jekyll templates: sites/**/*.html open with YAML front matter and carry Liquid tags inside their
         attributes, so they are not HTML documents. Sonar's HTML analyser would report the template syntax,
         not the page.

       Changed 2026-08-09: this line used to drop the two site directories whole, because those sites are
       written in their own session. That is a workflow reason, not a scope reason, and it hid the only public
       attack surface in the repo - 5e5f8c02 was an XSS fix in one of them. Back in scope: 6 hand-written js,
       15 scss, and the site yml and json. Fixing them is still not a coding session's job (CLAUDE.md);
       findings go in a plan for the session that owns those files. -->
  <Property Name="sonar.exclusions">**/node_modules/**,**/bin/**,**/obj/**,artifacts/**,results/**,assets/lib/**,ruby/vendor/**,ruby/*/spec/fixtures/**,sites/*/lib/**,sites/*/js/**,sites/*/media/**,sites/**/*.html,shared/data/**,vipaq/test-vectors/**</Property>

  <!-- One flat file per suite in artifacts/coverage/sonar, split by extension: .xml from the C# collector,
       .info from jest, .json from simplecov. tooling/tests.just is what writes them and the only place that
       knows the suite list.

       The ruby one opens with ../ and the other two do not. Sonar documents these paths as relative to the
       project root; what the 2026-08-27 run actually did was resolve them against the base directory of the
       module the sensor ran in. The C# and javascript sensors run in the root module, so root-relative is
       right for them. The ruby sensor runs in the only module holding .rb files - ruby/, its own project -
       and said so: "SimpleCov report not found: 'artifacts/coverage/sonar/*.json'".

       All three reports are in artifacts/coverage/sonar. Only the path written to reach them differs. -->
  <Property Name="sonar.cs.vscoveragexml.reportsPaths">artifacts/coverage/sonar/*.xml</Property>
  <Property Name="sonar.javascript.lcov.reportPaths">artifacts/coverage/sonar/*.info</Property>
  <Property Name="sonar.ruby.coverage.reportPaths">../artifacts/coverage/sonar/*.json</Property>

  <!-- Two places where near-identical code is the design, not debt. This is a duplication exclusion, not an
       issue ignore: every rule still runs on these files, only the copy-paste detector skips them.

       lib Algorithms: the v1/v2 variants of First/Best/Worst Fit Decreasing are separate implementations that
       happen to share a shape. Sonar read them at 93% duplicated, which was most of the 38% it reported for
       lib/ and the reason the gate first failed on duplication. Duplication elsewhere in lib/ still counts.

       api v3: the v3 API is frozen - no new endpoints, no behavioural changes, no contract edits, because it
       is a published contract and changing it breaks existing clients. v4 was therefore written beside it
       rather than on top of it, so every shared shape exists twice on purpose and the copy in v3 can never be
       removed. Excluding v3 also clears the v4 half of those pairs where v3 was the only other copy, which is
       right: that pairing is an artifact of the freeze. Duplication *within* v4 still counts, and it is real -
       the Fit/Pack endpoint pairs (PresetBin, PresetCompare, PresetSmallestBin) duplicate each other, and
       nothing stops those being factored out.

       Deliberately NOT excluded: the Npgsql/Sqlite repository pairs in ServiceModule.Infrastructure, at ~44%.
       Two backends implementing one interface could share a base with dialect-specific SQL, so that is real
       debt and stays visible. -->
  <Property Name="sonar.cpd.exclusions">lib/src/Binacle.Lib/Algorithms/**,api/src/Binacle.Net/v3/**</Property>

  <!-- Build tooling, not shipped code. The .NET support projects (benchmarks, performance suites, tools and
       the two test kernels) are handled properly instead, by SonarQubeTestProject in Directory.Build.props -
       that takes them out of the coverage denominator and off the product rule set in one move.

       SonarQubeTestProject only reaches MSBuild projects, so the five globs below do the same job for
       everything else: the python index generator, the site bundles, the ts fixture and generator folders,
       and the gem specs. The first four were 334 lines, every one uncovered and uncoverable.

       **/spec/** is the gems' own rspec suites - about half of ruby/. SimpleCov is configured to
       `skip '/spec/'`, so it reports nothing for them and they would sit at 0% forever.

       These stay in scope for rules. This is a coverage exclusion only. -->
  <Property Name="sonar.coverage.exclusions">gulpfile.js,jest.config.js,tooling/**,sites/**,**/tests/**,**/tools/**,**/spec/**</Property>

  <!-- No sonar.issue.ignore rules here, on purpose. Where a rule was wrong about our code the code now says
       so in a way the analyser can read - [AssertionMethod] on the helpers that assert, arrange/act/assert
       split so the check sits in the test body - rather than the finding being hidden from this file, where
       nobody reviewing the code would ever see it.

       The usual escape hatch, turning a rule off in the quality profile, is not open to us: custom quality
       profiles start at the Team plan and we are on Free, so "Sonar way" is fixed. A rule we disagree with is
       therefore answered in code, or marked Accepted on the individual finding in the SonarCloud UI, where it
       carries a reason and stays visible. Never with a blanket path ignore here. -->

</SonarQubeAnalysisProperties>
