namespace Binacle.Net.Kernel.Instance.Models;

// The feature is on and there is nothing more to say. Replaces the null path InstanceOptions used to hold for
// this case, so no value in the bag is ever null.
public sealed class SwitchedOn : FeatureValue
{
}
